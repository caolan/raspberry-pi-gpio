(compile -O2 -d1 -s "raspberry-pi-gpio.scm" -lwiringPi -Iinclude -j "raspberry-pi-gpio")
(compile -O2 -d0 -s "raspberry-pi-gpio.import.scm")

(install-extension
  'raspberry-pi-gpio
  '("raspberry-pi-gpio.so" "raspberry-pi-gpio.import.so")
  '((version "1.0.0")))
